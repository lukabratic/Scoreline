services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: scoreline
      POSTGRES_PASSWORD: scoreline
      POSTGRES_DB: scoreline
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
